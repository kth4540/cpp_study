#include <iostream>
using namespace std;

class intarr
{
private:
	int m_length = 0;
	int* m_data = nullptr;
public:
	//constructor	생성자
	//destructor	소멸자
	//initialize()	
	//reset();		메모리 초기화
	//resize();		사이즈 재설정
	//insertBefore(const int& val, const int& ix);	val을 m_data[ix]앞에 추가
	//remove(const int& ix);	m_data[ix] 제거
	//push_back(const int& val);	m_data 마지막 원소에 val 추가(메모리 크기 재설정)
};
int main()
{


	return 0;
}
